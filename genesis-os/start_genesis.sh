#!/bin/bash
# We use 'cage' to provide the display server (Wayland)
# This launches your app full screen, with no other UI elements.
cage /home/YOUR_USER/genesis-os/src-tauri/target/release/genesis-os